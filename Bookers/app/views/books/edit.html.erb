<h1>EditEditing Book</h1>
<%= form_with model:@book, url:"/books/#{@book.id}", local:true do |f| %>
 <div> <p class="label">title</p>
  <%= f.text_field :title %>
</div>
 <div> <p class="label">body</p>
  <%= f.text_area :body %>
</div>
<div>  <%= f.submit 'Updata Book' %>
</div>
<% end %>
<span>
  <p class="mini-menu"><%= link_to "Show", show_books_path(@book.id) %>|
  <%=link_to "Back",books_path%></p>
</span>